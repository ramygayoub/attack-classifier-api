apiVersion: v1
kind: Service
metadata:
  name: classifier-api
  labels:
    app: classifier-api
spec:
  type: NodePort
  selector:
    app: classifier-api
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
